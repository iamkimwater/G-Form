import { Text, View } from 'react-native'

const TitleComponent = () => {
  return (
    <View
      style={{
        flexDirection: 'row',
        alignItems: 'center',
        height: 50,
        margin: 5,
      }}
    />
  )
}
export default TitleComponent
