import { View } from 'react-native'

const QuestionAnswerComponent = () => {
  return (
    <View
      style={{
        flexDirection: 'row',
        alignItems: 'center',
        height: 50,
        margin: 5,
      }}
    />
  )
}
export default QuestionAnswerComponent
